# Exercise 12
"""
Palindrome Checker

Write a function `isPalindrome(text)` that returns `True` if the text is a palindrome, `False` if not.

Rules:
- Ignore spaces and punctuation
- Ignore capitalization
- `"racecar"` → True
- `"A man a plan a canal Panama"` → True
- `"hello"` → False

Then write `checkList(words)` that takes a list of words/phrases and prints each one with whether it is or is not a palindrome:
```
racecar → Palindrome
hello → Not a palindrome
A man a plan a canal Panama → Palindrome
```

Test list: `["racecar", "hello", "level", "Manali", "Was it a car or a cat I saw", "Python", "Madam"]`
"""
